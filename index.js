/**
 * @format
 */

import {AppRegistry} from 'react-native';
import AsycStorageScreen from './src/AsyncStorageScreen';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => AsycStorageScreen);
